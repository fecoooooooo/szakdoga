import { BooleanToHunPipe } from './boolean-to-hun.pipe';

describe('BooleanToHunPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanToHunPipe();
    expect(pipe).toBeTruthy();
  });
});
